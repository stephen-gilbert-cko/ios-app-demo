# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-app-demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-app-demo
  pod 'Frames', '~> 3.0'

  target 'ios-app-demoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-app-demoUITests' do
    # Pods for testing
  end

end
